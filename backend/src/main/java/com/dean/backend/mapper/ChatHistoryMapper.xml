<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dean.backend.mapper.ChatHistoryMapper">

    <insert id="insertChatHistory" parameterType="ChatHistory">
        INSERT INTO chat_history ( 
            conversation_id, 
            question, 
            answer)
        VALUES ( 
            #{conversationId}, 
            #{question}, 
            #{answer})
    </insert>

    <select id="selectChatHistoryList" resultType="ChatHistory">
        SELECT 
        id, 
        conversation_id AS conversationId,
        question, 
        answer, 
        created_at AS createdAt
        FROM chat_history
        WHERE conversation_id = #{conversationID}
        ORDER BY created_at ASC
    </select>

    <!-- 특정 conversationId의 이전 대화 조회 -->
    <select id="selectByConversationId" resultType="ChatHistory">
        SELECT
            id,
            conversation_id AS conversationId,
            question,
            answer,
            created_at AS createdAt
        FROM chat_history
        WHERE conversation_id = #{conversationId}
        ORDER BY created_at ASC
    </select>


</mapper>